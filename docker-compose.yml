version: "3.9"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: showukb-web
    restart: unless-stopped
    env_file:
      - .env.docker
    environment:
      DATABASE_URL: ${DATABASE_URL}
      PORT: 3000
    ports:
      - "3000:3000"
